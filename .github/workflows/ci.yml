name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck, test, web build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare env for build
        run: |
          cp .env.example .env
          cp apps/web/.env.example apps/web/.env

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build web
        run: pnpm build:web
        env:
          # Next build reads env files; CI has no secrets — use safe placeholders.
          DATABASE_URL: file:../../data/aperio-j-ci.db

  installers:
    name: Build desktop and mobile installers
    needs: verify
    if: github.event_name != 'pull_request'
    uses: ./.github/workflows/installers.yml

  publish:
    name: Publish installers to GitHub Releases
    needs: installers
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Prepare release filenames
        run: |
          mkdir -p release
          EXE=$(find release-assets -name '*.exe' | head -1)
          APK=$(find release-assets -name '*.apk' | head -1)
          test -n "$EXE" || { echo "Windows installer missing"; exit 1; }
          test -n "$APK" || { echo "Android APK missing"; exit 1; }
          cp "$EXE" release/Aperio-J-windows-setup.exe
          cp "$APK" release/Aperio-J-android.apk
          ls -la release/

      - name: Publish rolling latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest installers
          prerelease: true
          make_latest: true
          body: |
            Rolling CI build — updated on every push to `main`.

            | Platform | File | Notes |
            |----------|------|-------|
            | Windows | `Aperio-J-windows-setup.exe` | NSIS installer |
            | Android | `Aperio-J-android.apk` | Self-signed release APK — enable “Install unknown apps” on device |

            Android builds use the repo upload key for sideloading without an app store.
          files: |
            release/Aperio-J-windows-setup.exe
            release/Aperio-J-android.apk
          overwrite_files: true
